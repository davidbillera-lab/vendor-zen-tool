# Copy this file to .estatesales-test.env and fill in values.
# .estatesales-test.env is gitignored — never commit it.

# Supabase — VZT BACKEND project (NOT Mission Control). Optional for local tests:
# test-local.js sets AGENT_TEST_MODE=true, which disables the dedup ledger, so the
# agent never reads/writes Supabase. Only fill these in for a non-test-mode run.
SUPABASE_URL=https://atgrxqfxysvppqoyvjdd.supabase.co
SUPABASE_SERVICE_KEY=

# DOA admin auction page URL (EditAuction?id=...)
DOA_EMAIL=
DOA_PASSWORD=
DOA_URL=

# EstateSales sale URL + login fallback (email/password only needed if no es-session.json)
ESTATESALES_EMAIL=
ESTATESALES_URL=
